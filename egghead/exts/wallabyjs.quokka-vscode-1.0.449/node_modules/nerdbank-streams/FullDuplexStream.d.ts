/// <reference types="node" />
import { Duplex } from "stream";
export declare class FullDuplexStream {
    static CreatePair(): {
        first: Duplex;
        second: Duplex;
    };
    static Splice(readable: NodeJS.ReadableStream, writable: NodeJS.WritableStream): Duplex;
}
